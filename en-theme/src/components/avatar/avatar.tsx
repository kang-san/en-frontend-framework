import React from 'react';

import { AvatarProps } from './avatar.props';



export const Avatar = React.forwardRef<HTMLElement, AvatarProps>((props, ref) => {
  const {  } = props;

  return (
    <div></div>
  )
})