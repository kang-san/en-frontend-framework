
import React from 'react';

import { CardProps } from './card.props';

export const Card = React.forwardRef<React.ElementRef<'div'>, CardProps>((props, ref) => {

  return (
    <div>
      
    </div>
  )
})